// Import library

// Import components

const Dashboard = () => {
    return (
        <section className="flow">
            <h5>Dashboard</h5>
        </section>
    );
};

export default Dashboard;
